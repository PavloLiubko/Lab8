#include <iostream>
#include <cmath>
#include <iomanip>


int main()
{
    setlocale(LC_ALL, "RU"); // українського кодування на жаль не знайшов 
    
    
    std::string C, l;
    std::cout << "Виберіть початковий напрямок руху робота(W-захід/E-схід/N-півнчік/S-південь):";
    std::cin >> C;
    std::cout << "Скажіть що ви хочете, щоб робот зробив першим(0 - йшов прямо, 1 - повернув наліво, -1 - повернув направо, не рухатись - stop):";
    std::cin >> l;
    
    std::cout << "Якщо бажаєте зупинити рух в даному напрямку, введіть \"stop\" \n ";


    std::string W, E, N, S;
    if (C == "W") {
        while (l != "stop") {
            W += l;
            std::cout << "Які рухи будуть далі на Північному напрямку?:";
            std::cin >> l;
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на схід \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на східному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                E += l;
                std::cout << "Які рухи будуть далі на східному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на північ \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на північному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                N += l;
                std::cout << "Які рухи будуть далі на північному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на південному напрямку \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на південному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                S += l;
                std::cout << "Які рухи будуть далі на південному напрямку далі?:";
                std::cin >> l;
            }
        }
    } 
    if (C == "E") {
        while (l != "stop") {
            E += l;
            std::cout << "Які рухи будуть далі на східному напрямку?:";
            std::cin >> l;
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на захід \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на західному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                W += l;
                std::cout << "Які рухи будуть далі на західному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на північ \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на північному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                N += l;
                std::cout << "Які рухи будуть далі на північному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на південному напрямку \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на південному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                S += l;
                std::cout << "Які рухи будуть далі на південному напрямку далі?:";
                std::cin >> l;
            }
        }
    }
    if (C == "N") {
        while (l != "stop") {
            C += l;
            std::cout << "Які рухи будуть далі на північному напрямку?:";
            std::cin >> l;
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на захід \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на західному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                W += l;
                std::cout << "Які рухи будуть далі на західному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на північ \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на східному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                E += l;
                std::cout << "Які рухи будуть далі на східному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на південному напрямку \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на південному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                S += l;
                std::cout << "Які рухи будуть далі на південному напрямку далі?:";
                std::cin >> l;
            }
        }
        if (C == "S") {
            while (l != "stop") {
                S += l;
                std::cout << "Які рухи будуть далі на південному напрямку?:";
                std::cin >> l;
            }
            if (l == "stop") {
                std::cout << "Далі напрямок буде йти на захід \n";
                l = "";
                std::cout << "Як ви бажаєте почати рухатись на західному напрямку?(прицнип рухів не змінився):";
                std::cin >> l;
                while (l != "stop") {
                    W += l;
                    std::cout << "Які рухи будуть далі на західному напрямку далі?:";
                    std::cin >> l;
                }
            }
            if (l == "stop") {
                std::cout << "Далі напрямок буде йти на північ \n";

                l = "";
                std::cout << "Як ви бажаєте почати рухатись на північному напрямку?(прицнип рухів не змінився):";
                std::cin >> l;
                while (l != "stop") {
                    N += l;
                    std::cout << "Які рухи будуть далі на північному напрямку далі?:";
                    std::cin >> l;
                }
            }
        }
        if (l == "stop") {
            std::cout << "Далі напрямок буде йти на схід \n";
            l = "";
            std::cout << "Як ви бажаєте почати рухатись на східному напрямку?(прицнип рухів не змінився):";
            std::cin >> l;
            while (l != "stop") {
                E += l;
                std::cout << "Які рухи будуть далі на східному напрямку далі?:";
                std::cin >> l;
            }
        }
    }

    if (E == "") {
        E = "В цьому напрямку робот не рухався";
    }
    if (N == "") {
        N = "В цьому напрямку робот не рухався";
    }
    if (W == "") {
        W = "В цьому напрямку робот не рухався";
    }
    if (S == "") {
        S = "В цьому напрямку робот не рухався";
    }

    std::cout << "Ось вся схема пересування вашого робота: \n" << "На півночі: " << N << "\n" << "На півдні: " << S << "\n" << "На сході: " << E << "\n" << "На заході: " << W << "\n";



    return 0;
}

